# 이 파일을 .env.local 로 복사하고 값을 채우세요. .env.local 은 커밋되지 않습니다.
# 배포할 때는 같은 이름으로 Vercel 프로젝트 환경변수에 등록해야 합니다.

# Google AI Studio(https://aistudio.google.com/apikey)에서 발급받은 키.
# 없으면 카드뉴스 생성 요청이 500으로 실패합니다.
GEMINI_API_KEY=
