# 개발일지

작업 마디마다 이어 적는다. 최신 항목을 맨 위에 둔다.

무엇을 했는지보다 **왜 그렇게 했는지**를 남긴다. 무엇을 했는지는 `git log`에
있고, 나중에 다시 볼 때 아쉬운 쪽은 언제나 판단의 근거다.

---

## 2026-09-23 — 어두운 바탕 · 네온 템플릿과 색 조합

### 배경

디자인이 두 개뿐이라 뽑을 때마다 같은 인상이었다. 참고 자료 A 계열(거의 검정
바탕에 형광 한 색)을 넣기로 했는데, 요구가 하나 더 붙었다. **같은 형태로
색만 바꿀 수 있게 해 달라**는 것. 검정+핑크뿐 아니라 검정+형광그린,
검정+형광주황처럼.

### 결정과 근거

**색만 다른 것은 새 템플릿으로 만들지 않는다.** 검정+핑크와 검정+형광그린을
각각 `TEMPLATES` 에 넣으면 목록이 금세 색 견본으로 뒤덮인다. 고르는 사람
입장에서도 "디자인 일곱 개"와 "디자인 하나에 색 일곱"은 전혀 다른 화면이다.
그래서 축을 하나 더 뺐다. 템플릿은 레이아웃, `variant` 는 색이다.

`variants.ts` 의 배열에 `{ id, name, base, neon }` 한 줄을 더하는 것이 새 색
조합을 넣는 일의 전부다. 컴포넌트는 이 두 색만 보고 나머지를 계산한다.

**글자색을 흰색으로 박지 않았다.** 처음에는 어두운 바탕만 생각해서 흰 글자로
고정할 뻔했다. 그러면 "여러 가지로 베리할 수 있게 열어둬"라는 요구가 사실상
"어두운 색 중에서 골라라"가 된다. `inkFor(base)` 가 바탕 밝기를 보고 글자색
네 단계를 뒤집도록 해서, 아이보리 바탕 한 줄을 더해도 그냥 읽힌다. 실제로
`ivory-crimson` 을 넣어 확인했다.

**이 템플릿만 기사 색(`accent`)을 안 쓴다.** 앞선 두 템플릿은 모델이 고른
포인트 컬러가 배경을 만든다. 여기서는 바탕이 거의 검정으로 고정이고 네온
한 색이 그 위에서 혼자 튀는 것이 전부라, 기사마다 색이 달라지면 인상이
무너진다. 색은 사람이 고른다.

**고른 색은 결과에 붙여 보관한다**(`CardNewsEntry.variantId`). 템플릿 id 와
한 칸에 묶지 않고 따로 뒀다. 템플릿을 바꿨다 되돌아왔을 때 고르던 색이 그대로
있는 편이 자연스럽다.

### 겪은 문제

**상세 장의 아래쪽 절반이 통째로 비었다.** 처음에는 상세만 위에서부터 쌓아
장마다 글 시작 높이를 맞췄다. 넘길 때 덜 튈 거라고 생각했는데, 실제로 구워
보니 본문이 세 줄인 장은 화면 아래 절반이 그냥 빈 검정이었다. 의도가 아니라
실수처럼 보였다. 전부 세로 가운데로 바꿨다. 글이 조금 움직이는 편이 빈 화면
보다 낫다.

**밝은 바탕에서 글로우가 잉크 얼룩처럼 보였다.** 우하단 글로우 알파를 하나로
고정했더니 검정 위에서는 빛이 번지는데 아이보리 위에서는 분홍 얼룩이었다.
`Ink` 에 `glow` 값을 넣어 바탕 밝기에 따라 다르게 잡았다(어두우면 0.26,
밝으면 0.1).

**화살표는 글꼴 글자 대신 직접 그렸다.** 마무리 장의 아래 화살표를 `↓` 문자로
쓰면 PNG로 구울 때 글꼴에 그 글자가 없어 빠질 수 있다. 인라인 SVG 로 그려서
`html-to-image` 가 그대로 담게 했다.

### 일부러 규칙과 다르게 한 곳

`SKILL.md` 는 좌상단 배지를 **흰 배경에 어두운 글자인 둥근 알약**으로 쓰라고
한다. 이 템플릿은 알약을 쓰지 않고 네온색 글자만 놓는다. A 계열은 면을 쓰지
않고 선과 색으로만 위계를 만드는 디자인이라, 흰 알약을 하나 얹는 순간 그
원칙이 깨진다. 카테고리를 좌상단에 표시한다는 규칙의 목적은 지켰다.

### 남은 일

- **위계 네 단계 중 세 번째를 아직 못 만든다.** 참고 자료 A 는 한 장 안에서
  큰 흰 헤드라인 / 회색 설명문 / **흰 강조 문장** / 네온 강조 네 단계를 쓴다.
  우리는 `*...*` 하나뿐이라 네온 강조까지만 된다. "흰색 굵게"를 따로 지정하려면
  마커를 하나 더 만들어야 하고, 그러면 저장해 둔 결과와 프롬프트를 또 건드려야
  한다. 템플릿이 더 늘어나 정말 필요해질 때 한 번에 하기로 했다.
- **진짜 기사로는 아직 못 돌렸다.** 이 작업 환경에서 뉴스 사이트와 Gemini 로
  나가는 길이 막혀 있어, 손으로 쓴 MICE 기사 분량으로 다섯 장을 구워 확인했다.
  모델이 `*강조*` 를 어디에 거는지는 배포된 사이트에서 봐야 한다.
- **`accent` 를 따라가는 색 조합**(기사 색을 네온으로 쓰는 `auto`)은 넣지 않았다.
  `TemplateVariant` 가 색 두 개를 들고 있는 단순한 값이라는 점이 이 구조의
  전부인데, 거기에 "값이 없으면 바깥에서 가져온다"를 섞으면 그 단순함이 깨진다.

---

## 2026-09-23 — 낱말 단위 강조

### 배경

참고 자료 A 는 한 줄 안에서 일부 낱말만 색을 달리해 리듬을 만든다. 그런데
`heading`/`body` 가 `string[]` 이라 줄 단위로만 다룰 수 있었다. 이건 템플릿을
더 만들기 전에 정해야 하는 문제였다. 나중에 바꾸면 이미 만든 템플릿과
프롬프트를 전부 다시 손봐야 한다.

낱말 단위로 가기로 했다.

### 결정과 근거

**모델에게는 여전히 문자열 배열을 받는다.** 중첩 객체 배열
(`[[{text,accent}]]`)을 요구하면 모델이 형식을 틀리기 쉽고 토큰도 크게 는다.
대신 `국제회의 *47건*을` 처럼 별표로 감싸게 하고 우리가 쪼갠다.

이 선택의 값어치는 **응답 형식이 바뀌지 않는다**는 데 있다. 이미 저장해 둔
결과도 그대로 읽히고, 프롬프트 변경도 규칙 한 덩어리를 더하는 정도로 끝났다.

**쪼개는 일은 `normalizeCardNews` 안에서 한다.** 템플릿이 아니라 데이터가
들어오는 길목에서 한 번만 처리하면, 템플릿은 조각 배열만 알면 된다.

**저장소에서 꺼낼 때도 같은 함수를 통과시킨다**(`parseHistory`). 강조가 생기기
전에 저장한 결과는 줄이 그냥 문자열이라, 꺼낼 때 한 번 더 정리해 형식을 맞춘다.
`toLines` 가 문자열과 조각 배열을 모두 받아들이게 해서 같은 함수로 둘 다
처리한다.

**강조색은 밝기가 아니라 채도로 만든다.** 처음에는 포인트 컬러를 밝은 쪽으로
올려 썼는데(`shade(accent, 0.72)`), 옅은 하늘색과 흰색이 카드 크기로 줄여 놓으면
같은 색으로 보였다. 색상(hue)만 남기고 채도를 88%, 밝기를 68%로 고정하니 흰
글자 옆에서 확실히 다른 색으로 읽힌다.

**한 슬라이드에 한두 번만 쓰라고 프롬프트에 박았다.** 많이 쓰면 강조가 사라진다.
수치, 기관명, 판단을 가르는 낱말처럼 기억했으면 하는 것에만 쓰게 했다.

### 겪은 문제

없음. 다만 렌더된 조각 수를 세어 검증할 때 배지와 장수 표시의 `<span>` 까지
세어져 처음에는 강조가 없는 예전 결과에서도 4개가 잡혔다. 두 결과의 차이를
보고서야 강조 조각만 2개라는 것이 드러났다. **개수가 아니라 차이를 봐야 했다.**

### 남은 일

- 앞 항목의 남은 일은 그대로. 이제 참고 자료 A 를 템플릿으로 옮길 수 있다.
- 강조를 실제 기사로 돌려 보고 모델이 강조할 곳을 잘 고르는지 확인해야 한다.
  프롬프트로 지시했을 뿐 아직 진짜 기사로 시험해 보지 않았다.

---

## 2026-09-22 — 디자인 참고 자료 정리

### 배경

다음에 넣을 디자인 참고로 인스타그램 캡처 16장을 받았다. 대화는 세션이 끝나면
사라지므로 저장소에 적어 두지 않으면 잃는다. 지난번 mice-cardnews 저장소를
잊을 뻔한 것과 같은 종류의 문제다.

### 결정과 근거

**캡처 이미지는 저장소에 넣지 않고 글로만 적었다.** 남의 게시물이라 공개
저장소에 복제하는 것이 마땅치 않고, 색 값과 규칙을 글로 적어 두는 편이
구현할 때 오히려 쓸모 있다. `docs/template-references.md` 에 있다.

**구현 전에 막히는 지점 두 개를 미리 적어 뒀다.** 나중에 착수해서 발견하면
설계를 되돌려야 한다.

- 받은 자료는 한 줄 안에서 **일부 단어만** 색을 바꾼다. 지금
  `heading`/`body` 는 `string[]` 이라 줄 단위로만 다룰 수 있다. 단어 단위
  강조를 하려면 데이터 형식과 프롬프트를 함께 바꿔야 한다. 줄 단위 강조만
  하면 훨씬 싸다. **어디까지 필요한지 먼저 정해야 한다.**
- 카카오톡·노션·웹사이트를 흉내낸 스타일은 디자인이 아니라 **콘텐츠 구조가
  다르다**. 대화, 체크리스트, 섹션 묶음은 `heading[] + body[]` 로 표현되지
  않는다. 껍데기만 흉내내고 평범한 문단을 넣으면 어색해진다. 템플릿마다 다른
  데이터 모양과 다른 프롬프트가 필요하다는 뜻이라, 나머지보다 훨씬 큰 작업이다.

**`BackgroundKind` 에 `mockup` 을 아직 넣지 않았다.** 쓰는 템플릿이 하나도
없는 상태에서 타입만 늘리면 죽은 코드가 된다. 실제로 만들 때 넣는다.

### 덧붙임 — 규칙 자료 9장을 더 받았다

앞서 받은 16장이 베낄 **디자인 견본**이었다면, 이번 9장은 만드는 법을 설명한
**규칙**이다. 옮겨 그릴 것이 없는 대신 지금 구현과 숫자를 맞대 볼 수 있어서
성격이 다르다. 참고 문서에 C·D 로 나눠 적었다.

가장 쓸모 있는 것은 1080×1350 기준의 구체적인 글자 크기다. 대조해 보니
**우리가 전반적으로 크다**(표지 헤드라인 92px 대 60p, 본문 38px 대 28p).
다만 가이드의 판에는 사진이 자리를 크게 차지하고 우리는 글자만 있다. 같은 칸을
글자가 다 쓰니 커지는 것이 자연스럽다. **사진을 넣게 되면 그때 내려야 한다.**

그대로 받아들이지 않기로 한 것도 적어 뒀다. 1080×1440 규격은 인스타그램 피드가
자르지 않고 보여주는지 확인하지 못했고, "선물 약속 후 챗봇" 마무리는 리드를
모으는 마케팅 패턴이라 공공기관 뉴스에 맞지 않는다.

추천 폰트로 나온 프리텐다드는 npm 에 OFL-1.1 로 있어 넣을 수는 있다. 다만 지금
쓰는 원티드산스도 같은 모던 고딕 계열이라 바꿀 이유가 아직 없다.

### 남은 일

- 앞 항목의 남은 일은 그대로.
- 어두운 단색 + 단일 포인트 컬러 연작(참고 자료 A)은 지금 구조로 바로 만들 수
  있다. 다음 차례로 가장 싸고 효과가 크다.
- 본문 줄 수를 4줄에서 3줄로 줄일지 정한다. 프롬프트와 `toLines` 상한만
  고치면 되는 작은 일이다.

---

## 2026-09-16 — 카드 템플릿 시스템, 명조 추가

### 배경

디자인이 하나뿐이라 몇 번 만들면 다 같아 보였다. 참고할 인스타그램 캡처 8장을
받았는데, **8장 전부 사진 위에 글씨를 얹는 형식**이었다. 우리는 저작권 때문에
사진을 쓰지 않으므로 그대로는 옮길 수 없었다.

### 결정과 근거

**템플릿을 "배경 × 글꼴" 두 축으로 나눴다.** 디자인을 하나하나 별개로 두면
늘어날수록 관리가 안 된다. 실제로 갈리는 지점이 "배경을 무엇으로 채우는가"와
"어떤 계열 글꼴을 쓰는가"라서 그 둘만 축으로 두고 나머지는 템플릿 안에 뒀다.

**사진형과 색 배경형을 섞지 않는다.** 받은 샘플의 글자 테두리와 그늘은 사진이
복잡해서 생긴 장치다. 같은 처리를 단색 배경에 쓰면 근거 없이 요란해진다.
그래서 `BackgroundKind`로 갈라 두고, 사진이 없으면 사진형은 아예 목록에
나오지 않게 했다(`availableTemplates`). 쓸 수 없는 것을 보여주고 눌렀을 때
실망시키는 것보다 낫다.

**명조는 나눔명조를 골랐다.** 본명조가 가장 중립적이지만 두 굵기에 약 1.95MB로
나눔명조의 두 배다. PNG로 내보낼 때 폰트를 파일에 통째로 심으므로 이 차이가
그대로 체감된다. 나눔명조는 신문 활자 계열이라 뉴스 카드뉴스라는 용도에 톤도
맞고, 원티드산스(기하학적 모던 고딕)와 대비가 확실해서 다른 템플릿이라는
느낌이 분명히 남는다.

**명조 템플릿은 반투명 카드를 쓰지 않는다.** 기존 것과 최대한 다르게 잡으려고
단색 배경에 글을 바로 올리고, 중앙 대신 좌측 상단에 붙였다. 같은 기사로 둘을
번갈아 보면 다른 세트처럼 보이는 것이 목적이다.

**고른 템플릿을 결과와 함께 보관한다**(`CardNewsEntry.templateId`). 화면
상태로만 두면 다른 결과를 보다가 돌아올 때마다 기본값으로 되돌아간다.

### 겪은 문제

fontsource 의 한글 폰트는 unicode-range 로 쪼갠 파일이 굵기당 100개가 넘는다.
그대로 쓰면 PNG를 구울 때 임베드할 `@font-face` 가 수백 개가 된다. 통짜
`korean` woff2 하나와 `latin` woff2 하나만 가져다 직접 선언했다. 겹치는
글자는 나중 선언이 이기므로 한글을 먼저, 라틴을 뒤에 둬서 숫자와 알파벳이
라틴 전용 파일로 그려지게 했다.

### 남은 일

- **사진 파이프라인** — 받은 샘플을 제대로 살리려면 이게 선행 조건이다.
  Pexels API(키 발급 즉시, 출처 표기 의무 없음)와 직접 업로드를 함께 두는
  방향으로 정리했다. 라이선스 정보를 `TemplatePhoto.credit` 필수 항목으로
  박아 두어 출처 불명 이미지가 들어올 여지를 코드에서 막았다.
- 색 배경 디자인은 사용자가 더 추가하기로 했다.
- 명조 템플릿의 상세 장은 본문이 짧으면 아래 여백이 크게 남는다. 위에서부터
  쌓는 구조라 의도한 것이지만, 실제로 써 보고 어색하면 조정한다.

---

## 2026-09-16 — 생성 결과 영속화

### 배경

`sessionStorage`에 담아 두어서 새로고침 한 번에 결과가 사라졌다. 20~40초를
들여 만든 것이 탭을 잘못 닫으면 없어졌고, 전에 만든 카드뉴스를 다시 열 방법도
없었다.

### 결정과 근거

**서버 데이터베이스 대신 `localStorage`를 골랐다.** 실제로 불편한 것은
"새로고침하면 날아간다"였고 그건 브라우저 저장소로 해결된다. 데이터베이스를
붙이면 Vercel에서 설정할 것이 늘고 비용도 생긴다. 얻는 것에 비해 치르는 값이
크다.

대신 **링크 공유는 못 한다.** 다른 기기나 다른 사람에게 주소를 보내도 열리지
않는다. 그게 필요해지는 시점이 데이터베이스를 붙일 시점이다.

**결과마다 id를 주고 `/preview?id=...` 로 연다.** 마지막 것 하나만 보관하면
새로고침은 해결되지만 이전 결과로 돌아갈 수 없다. id가 있으면 주소를 북마크할
수도 있다.

**로딩 화면에서 `router.push` 대신 `replace` 를 쓴다.** 미리보기에서 뒤로
가기를 누르면 로딩 화면으로 돌아가 생성이 다시 도는 문제가 있었다. 기록을
남기지 않으면 뒤로 가기가 홈으로 간다.

**보관 개수를 20건으로 제한했다.** 한 건이 2~4KB라 20건이면 100KB 이하다.
localStorage 한도(대략 5MB)에 한참 못 미치면서, 무한정 쌓여 언젠가 한도에
부딪히는 일도 없다.

### 겪은 문제

`useSearchParams` 를 쓰는 화면은 Suspense 경계 안에 있어야 한다. 미리 렌더되는
경로에서 경계가 없으면 프로덕션 빌드가 실패한다. 개발 모드에서는 통과해서
모르고 지나가기 쉽다. `PreviewContent` 를 분리하고 기본 내보내기에서 감쌌다.

localStorage 는 시크릿 창이나 사이트 데이터 차단 상태에서 접근만 해도 예외를
던진다. 읽기와 쓰기를 모두 try/catch 로 감싸, 그런 환경에서도 보관만 안 될 뿐
생성과 화면은 정상 동작하게 했다.

### 남은 일

- 디자인 템플릿 확장이 다음 차례다. 받은 참고 이미지가 전부 사진 위에 글씨를
  얹는 형식이라, 사진 파이프라인이 선행 조건이 된다.
- 링크 공유가 필요해지면 서버 저장으로 옮긴다.

---

## 2026-08-31 ~ 09-03 — Instagram 전용 카드뉴스로 재편, 배포

### 배경

기존 코드는 인스타그램 카드뉴스와 스레드 타래를 함께 만드는 구조였는데
원하는 품질이 나오지 않았다. 원인을 찾아보니 콘텐츠 파이프라인 전체가
문제였다.

- 결과물을 이미지로 저장하는 기능이 아예 없었다. "이미지 ZIP" 버튼에
  `onClick`이 없었고 관련 의존성도 없었다.
- 슬라이드 배경을 `image.pollinations.ai`에서 즉석 생성했고, 실패하면
  `picsum.photos` 랜덤 사진으로 대체했다. 기사와 무관한 사진이 붙었다.
- 카드가 1:1 정방형이라 인스타그램 세로 피드 규격(4:5)이 아니었다.
- 프롬프트가 "할루시네이션 금지"라고 해놓고 바로 아래에서 성과 지표
  숫자를 지어내라고 시키고 있었다.
- 테마 선택 페이지(`/selection`)에 도달할 경로가 없었다. 랜딩이 곧장
  `/loading`으로 보냈다.

### 결정과 근거

**웹앱을 새로 짜지 않고 엔진만 교체했다.** UI 골격(랜딩·로딩·미리보기)은
완성도가 있었고 빌드도 깨끗했다. 망가진 쪽은 콘텐츠 파이프라인이었다.
집은 두고 안의 기계만 바꾸는 편이 싸다.

**플랫폼을 인스타그램 하나로 고정했다.** 프롬프트와 미리보기 레이아웃이
각각 두 벌이라 고칠 곳이 두 배였다. 하나를 버리니 관리 범위가 절반이 됐고,
`platform` 파라미터 자체가 사라지면서 `"Instagram"`(대문자)과
`"instagram"`(소문자) 비교가 어긋나 미리보기가 통째로 빈 화면이 되던
문제도 함께 없어졌다.

**제작 규칙은 `mice-cardnews` 스킬을 그대로 따랐다.** 1080×1350, 원티드산스,
반투명 다크 카드, 패러프레이즈, 저작권 규칙. 규칙 대 구현 위치 대조표는
README에 있다.

**줄바꿈은 `<br>` 대신 줄 배열로 받는다.** 스킬은 `<br>`로 끊으라고 하지만,
React에서 HTML 문자열을 주입하지 않으려면 배열이 낫다. `heading`과 `body`가
`string[]`이고 한 원소가 화면의 한 줄이 된다. 효과는 같다.

**사진을 아예 쓰지 않기로 했다.** 스킬이 허용하는 Unsplash를 붙이려면 키가
하나 더 필요하고 실패 경로도 는다. 무관한 사진이 붙는 것보다 그라데이션과
타이포그래피가 낫고, 이건 스킬이 명시한 대체 방식이기도 하다.

**PNG는 브라우저에서 굽는다.** 서버 렌더링(`next/og` + satori)은 폰트 번들
500KB 제한이 있는데 원티드산스 TTF 한 종이 2.4MB다. `html-to-image`로
미리보기와 같은 DOM을 캡처하면 보이는 것과 저장되는 것이 항상 같다.

**원티드산스를 저장소에 넣었다(OFL-1.1).** CDN에서 불러오면 PNG로 구울 때
CORS 때문에 글자가 깨질 수 있다. 같은 출처여야 안전하다. 카드가 쓰는 굵기는
Regular/Bold/ExtraBold 세 종뿐이라 woff2로 약 2MB다.

**지어낸 성과 지표를 제거했다.** 데이터 형식을 새로 정의하면서 그대로 옮길
수 없었다. 할루시네이션을 금지하는 프롬프트에 숫자를 지어내라고 적는 건
알면서 거짓을 넣는 일이다. 랜딩의 "발행 전 성과 예측" 섹션도 광고할 대상이
없어져 제작 규칙 소개로 바꿨다. 근거 없던 "AI 정확도 98%", "3초 만에",
"첫 50개 무료"도 같이 정리했다.

### 겪은 문제

**빌드는 성공하는데 사이트가 404였다.** Vercel 프로젝트의 Framework Preset이
`Other`로 잡혀 있었다. 이 경우 Output Directory 기본값이 "`public` 폴더가
있으면 `public`, 없으면 `.`"이라, `next build`는 돌지만 그 결과물 대신
`public/`을 정적 사이트로 서빙한다. `public/`에 `index.html`이 없으니 전부
`NOT_FOUND`가 된다.

카드용 폰트를 `public/fonts/`에 넣으면서 `public/`이 생긴 것이 방아쇠였다.
그 전에는 폴더가 없어 루트를 서빙해 넘어가고 있었다.

대시보드를 고치는 대신 `vercel.json`에 `"framework": "nextjs"`를 적었다.
이 값이 대시보드 설정보다 우선하므로 저장소만 보고도 항상 올바르게 빌드된다.
나중에 프로젝트를 새로 만들어도 재발하지 않는다.

**다운로드 파일 이름이 사라졌다.** 링크를 문서에 붙이지 않고 클릭한 뒤
`URL.revokeObjectURL`을 즉시 호출했다. 브라우저가 blob을 읽기 전에 URL이
없어져 `download` 속성이 무시됐다. 링크를 `body`에 붙이고 해제를 미뤄
해결했다.

### 남은 일

- **원문 대조 검수** — 문장이 기사를 그대로 옮기지 않았는지는 사람이 원문과
  나란히 놓고 확인해야 한다. 프롬프트로 지시했을 뿐 검증 수단이 없다.
- **디자인 템플릿 추가** — 지금은 레이아웃이 하나뿐이다. 표지 샘플을 받아
  템플릿 시스템(`레이아웃 × 팔레트`)으로 확장할 예정.
- **Unsplash 사진** — 저작권 안전한 실사 사진. 키가 필요하다.
- **결과 영속화** — `sessionStorage`에만 있어 새로고침하면 사라진다.
- **lint 에러 2건** — `tailwind.config.js`의 `require()`. Tailwind v3 설정
  파일 형식이라 그대로 둔다.
